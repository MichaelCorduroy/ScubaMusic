package com.example.scubamusic0_0_1;

/*
import java.io.File;
import java.io.FileInputStream;
import java.io.IOException;
import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.util.Objects;

import com.google.auth.oauth2.GoogleCredentials;
import com.google.firebase.FirebaseApp;
import com.google.firebase.FirebaseOptions;
import com.google.firebase.database.FirebaseDatabase;
//import com.google.firebase.firestore.FirebaseFirestore;


public class databaseChecker {
    public  void main(String[] args) throws IOException, ClassNotFoundException {
        // Replace these values with your actual database details


        FileInputStream serviceAccount = new FileInputStream("src/main/resources/scubakey.json");

        // Create FirebaseOptions (using latest approach - builder method)
        FirebaseOptions options = FirebaseOptions.builder()
                .setCredentials(GoogleCredentials.fromStream(serviceAccount))
                .build();

        FirebaseApp.initializeApp(options);

        // Get an instance of Firestore
        //FirebaseFirestore db = FirebaseFirestore.getInstance();

        // Use the db instance to perform CRUD operations (see next steps)

        System.out.println("Successfully connected to Firestore!");
    }
}
*/